import React, { Component } from "react";

class Auth extends Component {
    render() {
        return (
            <div>
                <h2>Login / Sign Up</h2>
            </div>
        );
    }
}

export default Auth;